ok, now we need a new subsystem in the daemon, the brain of merlin. the daemon will actively engage with selected projects / sessions. the raw representations of these are read from disk from ~/.claude/projects/.../<session-id>.json. Some of these are user initiated, some are subagents. Here is how I see the workflow: when I as a dev / architect send a prompt and will be watching it being acted on, I will likely need lots of infos to learn about the system being built, discover missing pieces, issues, edge cases, etc. This is the level / granularity of the ~/.claure/projects/... files. Great. But typically what happens is that a bunch of agents work in many sessions over several days (with active dev/architect support), and we get to a state with the system built that the architect questions: too much tech debt, my picture about what is needed changed (got much better, refined) and I get the feeling of a big rewrite coming. Now I want to collect what's important and what should be kept and separate that from the mistakes made or things that in retrospect I want to change. Here I will want to look back on all sessions, like what did I exactly ask in the user prompt, but on others on much coarser granularity, esp. the agent's responses (maybe a really short summary) and tool calls (literally don't care), subagent was launched for X (the starting prompt of this is as interesting as a user prompt). I want to rebuild my mental model and create a new master prompt with all the important stuff of the previous days' prompts and decisions. So to support this rearchitect-workflow, we will need to create really concise summaries of long agent responses, re-segment multi-day user-agent sessions into coherent pieces (as the architect likely reused / abused sessions for unrelated stuff). This is where I expect LLMs to ace and also this product to deliver something novel. Let's brainstorm about this process, what data structures we will need for the segmented, simplified, requirement-oriented rearrangement of the previous sessions, how merlin can support the architect in showing the raw timeline vs showing a processed view or perhaps moving back and forth on this spectrum. This is the brain of merlin in the daemon, and I want to have some visual representation (in the mobile app and / or a rich desktop-native app) but also have a model / context in the daemon so that I can talk to an LLM about it and gradually get to a clean rewriting plan with multiple prompts/agents that I can confidently execute. Of all these summaries that we will create likely with lightweight, cheaper llms, what the lifetime of those are, like do we summarize upfront and store, vs do it on the fly later, all important questions.
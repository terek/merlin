/**
 * Re-exports from @merlin/protocol.
 * Web client components import from here via the @/ alias.
 */

export type { ClerkMessageEntry, ClerkToolDef, ClientMessage, DaemonMessage } from '@merlin/protocol'
